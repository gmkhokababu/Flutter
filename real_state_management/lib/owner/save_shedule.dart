

import 'package:flutter/material.dart';

class SaveShedule extends StatefulWidget {
  const SaveShedule({super.key});

  @override
  State<SaveShedule> createState() => _SaveSheduleState();
}

class _SaveSheduleState extends State<SaveShedule> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child:Text("Hello"),),
    );
  }
}
